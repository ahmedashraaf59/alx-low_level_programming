#include "main.h"
/**
 * print_sign - fumction that prints signs
 *
 * @n function parameter
 * Return: 1,0,-1
 */
int print_sign(int n)
{
	if (n > 0)
		_putchar ('+');
	return (1);
	if else (n = 0);
		_putchar ('0');
	return (0);
	if else
	_putchar ('-');
	return (-1);
}
